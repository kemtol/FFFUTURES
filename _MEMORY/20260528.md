# 2026-05-28 — MNQ ORB Research Checkpoint

## Context

The active research track moved to **MNQ only**. MGC/Super Structure remains in
the repo as historical/live context, but today's research artifacts and
documentation updates are for MNQ ORB.

## Data And Namespace

MNQ research paths:

```text
pipeline/mnq_ml/
data/Level_0_Raw/MNQ_1m.duckdb
data/Level_1_Features/mnq/
data/Level_2_Datamart/mnq/
model/MNQ/
```

Current ORB experiment:

```text
pipeline/mnq_ml/experiments/ORB/
data/Level_1_Features/mnq/ORB/context.parquet
data/Level_2_Datamart/mnq/ORB/events.parquet
data/Level_2_Datamart/mnq/ORB/sweeps/
```

## Iteration Log

### Iteration 0: M1 Baseline

Hypothesis:

```text
MNQ opening range breakout after NY cash open has intraday continuation edge.
```

Baseline implemented:

```text
Source: MNQ_1m.duckdb
OR: 30m
Side: long only
Entry: first M1 close above OR high, enter next M1 open
Exit: 15:00 NY
Sizing: fixed target risk using OR low as reference
```

Gate:

```text
L1 rows: 2,487,265
L2 rows: 637
L1 audit: PASS
Training gate: PASS
```

Finding:

```text
Risk $200 made 2026 trade frequency too low because many opportunities had
risk_per_contract_usd > target_risk_usd, producing contracts_used < 1.
```

### Iteration 1: OR / Side / Risk Sweep

Worker:

```text
pipeline/mnq_ml/experiments/ORB/sweep_orb_params.py
```

Grid:

```text
orb_minutes: 10, 15, 20, 30
side_mode: long, short, long_short
target_risk_usd: 100, 200, 300, 400, 500, 600
exit_mode: time_exit
```

Finding:

```text
Long-only was cleaner than short-only or long_short on full-history behavior.
30m long risk $600 had strong full-history return/DD but was weak in recent
30D. 15m long was more promising for a Topstep-style 30-day evaluation.
```

### Iteration 2: TP 2R Or Time Exit

Change:

```text
If trade reaches +2R before 15:00 NY, close immediately.
Otherwise close at 15:00 NY.
```

Expanded grid:

```text
exit_mode: time_exit, tp_2r_or_time
result_rows: 144
event_rows: 159,294
```

Best current candidate:

```text
15m OR
long only
TP 2R or 15:00 NY
target_risk_usd: 500
```

Evidence:

| Window | Trades | PnL | Max DD |
| ---: | ---: | ---: | ---: |
| 30D | 18 | $3,491 | -$549 |
| 50D | 30 | $5,448 | -$859 |
| 100D | 54 | $4,135 | -$4,066 |
| 200D | 94 | $5,569 | -$4,556 |

Aggressive neighbor:

```text
15m OR, long only, TP 2R/time, risk $600
30D: $3,581 / DD -$823
50D: $6,222 / DD -$859
100D: $4,414 / DD -$5,014
200D: $6,322 / DD -$5,014
```

## Decision

Keep the current candidate as a research candidate:

```text
15m long TP 2R/time risk $500
```

Do not promote to live. The next gate is Topstep-specific evaluation.

## Next Steps

P0:

- Add daily consistency metrics.
- Add rolling 30D MLL breach approximation.
- Create a candidate report comparing risk $400/$500/$600.
- Confirm MNQ commission/slippage assumptions.

P1:

- Sweep TP 1.5R / 2R / 2.5R / 3R.
- Sweep time exits 14:00 / 15:00 / 15:30 / 16:00 NY.
- Add year-by-year robustness view.

P2:

- Only if P0/P1 survive, add feature filters or ML.
