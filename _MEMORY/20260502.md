# 2026-05-02 Research Memory

_Session: continuity checkpoint after live calibration, TradingView comparator work, and Databento data repair._

---

## Current State

The active research branch is no longer ORB_v1.0. Current active model family is:

```text
model/ORB_v2.0_2010-2026/
```

Model state:

| Item | Status |
|---|---|
| Active feature count | 42 |
| Active modules | 7 (`interaction`, `macro`, `orb_context`, `pre_breakout_profile`, `scale_invariant`, `session_momentum`, `volatility_normalized`) |
| Models | 20 LGBM files, rev+cont for all 10 labels |
| Final train through | 2025 |
| Holdout start used by Topstep pass eval | 2025-12-01 |
| Main target deployed live | `y_1r4_180m` |
| Live risk | `$100 / 1R` |

Topstep 50K simulation snapshot from `model/ORB_v2.0_2010-2026/topstep_pass_results.csv`:

| Target | Score | Pass | Fail MLL | Median PnL | Avg Trades |
|---|---:|---:|---:|---:|---:|
| `y_1r2_close60m` | +0.8795 | 87.95% | 0.0% | $5,377 | 29.4 |
| `y_1r4_180m` | +0.8795 | 87.95% | 0.0% | $4,189 | 42.1 |
| `y_1r4_120m` | +0.8554 | 85.54% | 0.0% | $4,977 | 39.7 |
| `y_1r4_240m` | +0.8434 | 84.34% | 0.0% | $5,241 | 38.0 |

Important interpretation:

- ORB_v2.0 is a simulation GO candidate, not yet live-validated.
- Live/paper results are currently poor and must be treated as evidence that execution/data-source calibration is still the bottleneck.
- Do not roll forward confidence from Topstep sim into live trading without reconciling live trade outcomes.

---

## Latest Live / Paper State

Process check:

```text
python3 -u pipeline/live/runner.py --live
```

is running as PID `207072` and listening on port `8080`.

Latest `data/Live/daemon.log` tail shows repeated heartbeat lines:

```text
[Live] Alive | loop=23580 | signals=3 | Events: 3 | Signals: 3 | Signal rate: 100.0% close=4626.3
```

Portfolio state from `data/Live/portfolio_state.json`:

| Metric | Value |
|---|---:|
| Start balance | $50,000 |
| Current balance | $49,176 |
| Closed trades | 8 |
| Open positions | 0 |
| Closed trade PnL | 8 x -$103 |

Closed live/paper trades:

| # | UTC Open | Direction | Session | Entry | Result |
|---:|---|---|---|---:|---:|
| 1 | 2026-04-29 07:19 | LONG | london | 4590.5 | -$103 |
| 2 | 2026-04-29 13:39 | LONG | us | 4539.4 | -$103 |
| 3 | 2026-04-30 00:45 | SHORT | tokyo | 4582.1 | -$103 |
| 4 | 2026-04-30 07:07 | SHORT | london | 4604.2 | -$103 |
| 5 | 2026-04-30 13:36 | LONG | us | 4637.4 | -$103 |
| 6 | 2026-05-01 00:13 | SHORT | tokyo | 4634.0 | -$103 |
| 7 | 2026-05-01 07:06 | LONG | london | 4602.5 | -$103 |
| 8 | 2026-05-01 13:36 | SHORT | us | 4614.1 | -$103 |

This is an 8-loss streak after deployment. That is not compatible with the simulated pass-rate profile and should be investigated before any size increase or confidence upgrade.

Operational notes:

- `data/Live/signal_log.txt` still contains older restart/import errors (`datetime.timedelta`, `data_buffer` import, `database is locked`) before the current runner settled.
- `daemon.log` heartbeat is currently stable.
- Runner signal counters in heartbeat (`signals=3`) do not match the persisted portfolio count (`closed_trades=8`), likely because the runner instance state was reset while portfolio state persisted. Use `portfolio_state.json` for trade outcome continuity.

---

## Batch vs Live Feature Calibration

Latest module report from `model/CALIBRATION/module_report.csv`:

| Module | Match % `<0.01` | Mean Abs Delta | Features |
|---|---:|---:|---:|
| `core` | 100.0% | 0.000000 | 3 |
| `scale_invariant` | 100.0% | 0.000002 | 7 |
| `volatility_normalized` | 100.0% | 0.000000 | 5 |
| `session_momentum` | 100.0% | 0.000004 | 4 |
| `interaction` | 100.0% | 0.000002 | 5 |
| `pre_breakout_profile` | 98.63% | 0.003406 | 5 |
| `orb_context` | 97.31% | 0.052301 | 7 |
| `macro` | 90.53% | 0.008297 | 4 |

Interpretation:

- Deterministic feature replication is mostly solved at module level.
- Remaining meaningful deltas are `orb_context` and `macro`.
- The live 8-loss streak suggests module-level calibration is not sufficient; next investigation should compare full live decisions/trades against batch-equivalent decisions at the exact live event timestamps and data source.

---

## TradingView Strategy Comparator

`pipeline/live/tv_strategy.py` and `pipeline/live/compare_tv_trades.py` now match TradingView strategy semantics closely after indicator and lifecycle fixes.

Current artifact:

```text
model/CALIBRATION/tv_strategy_comparison.json
```

Summary:

| Metric | Value |
|---|---:|
| Comparable trades | 33 |
| Match rate | 96.97% |
| Closed trades | 32 match |
| Remaining item | trade #61 entry-only open |

Critical fixes already made:

- TradingView timestamps normalized to UTC in `data/Live/tv_trades.json`.
- CCI source uses `hl2`.
- DEMA, CCI, Supertrend, and ADX/DMI formulas match TradingView behavior.
- Execution lifecycle matches TradingView order semantics: prior stop active first, trend-flip close after stop check, then next-bar dynamic stop, with same-bar exit/re-entry allowed.

---

## Databento Repair

Importer created:

```text
pipeline/fetch/ingest_databento_json.py
```

Input:

```text
data/Level_0_Raw/databento/glbx-mdp3-20260416-20260430.ohlcv-1m.json
```

Behavior:

- Reads JSONL OHLCV.
- Ignores spread symbols containing `-`.
- Selects highest-volume outright per minute.
- Replaces only the input timestamp range in `MGC_1m.db`.
- Does not wipe historical raw data.

Last recorded ingest:

| Metric | Value |
|---|---:|
| Rows seen | 27,956 |
| Spreads skipped | 12,534 |
| Inserted/replaced | 15,180 |
| Range | 2026-04-16 00:00:00 -> 2026-04-30 23:59:00 |

Backups exist:

```text
data/Level_0_Raw/MGC_1m.db.bak_pre_json_ingest
data/Level_0_Raw/MGC_5m.db.bak_pre_json_ingest
data/Level_0_Raw/MGC_15m.db.bak_pre_json_ingest
data/Live/topstepx_buffer.db.bak_pre_databento_overlay
```

Data design risk remains:

- `DataBuffer` writes different providers into final OHLCV tables without source provenance.
- Bad candles can become sticky.
- Recommended next architecture is source-specific raw tables plus a canonical priority merge:

```text
raw_databento_1m
raw_yfinance_1m
raw_topstep_1m
canonical_ohlcv_1m = priority merge + source + quality_flag
```

---

## Highest-Priority Next Work

1. **Explain the 8-loss live streak**
   - Reconstruct each live event from `portfolio_state.json`.
   - Rebuild batch-equivalent features at the same event timestamps.
   - Compare model probabilities, decision (`rev`/`cont`/`skip`), TP/SL, and data source candles.

2. **Separate live data provenance**
   - Stop relying on mixed final OHLCV state as the only truth.
   - Add `source` and `quality_flag`, or split raw tables before canonical merge.

3. **Validate live execution semantics**
   - Confirm TopstepX order side, stop placement, TP/SL placement, and auto-close behavior.
   - Confirm whether paper/live closes are simulated by `portfolio.py` only or also mirrored by real TopstepX orders.

4. **Refresh post-repair research data**
   - If Databento-repaired Apr 16-Apr 30 data should become canonical, rebuild Level 1 and Level 2 artifacts before making new research claims.

5. **Keep TradingView comparator separate**
   - The TV ST/DEMA/ADX/CCI strategy comparator is now calibrated, but it is not the ORB_v2 model.
   - Do not mix its trade-match success with ORB_v2 live readiness.
